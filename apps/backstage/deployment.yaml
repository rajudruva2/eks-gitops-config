apiVersion: apps/v1
kind: Deployment

metadata:
  name: backstage
  namespace: backstage

spec:
  replicas: 1
  strategy:
    type: Recreate
  
  selector:
    matchLabels:
      app: backstage

  template:
    metadata:
      labels:
        app: backstage

    spec:
      serviceAccountName: backstage-kubernetes
      containers:
        - name: backstage

          image: rajvam6806/backstage:fccf14f28763029e07a7fba3ed02021e2b8f2840

          ports:
            - containerPort: 7007

          env:
            - name: NODE_ENV
              value: production
            - name: BACKSTAGE_DB_MIGRATIONS_SKIP
              value: "true"
                
          envFrom:
            - secretRef:
                name: backstage-secret

            - secretRef:
                name: backstage-github-auth    
