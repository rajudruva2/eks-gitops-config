apiVersion: apps/v1
kind: Deployment

metadata:
  name: backstage
  namespace: backstage

spec:
  replicas: 1
  strategy:
    type: Recreate
  
  selector:
    matchLabels:
      app: backstage

  template:
    metadata:
      labels:
        app: backstage

    spec:
      serviceAccountName: backstage-kubernetes
      containers:
        - name: backstage

          image: rajvam6806/backstage:7f1e7a4d50bdea36b0e2b86b5815bb0a2ee52014

          ports:
            - containerPort: 7007

          env:
            - name: NODE_ENV
              value: production

                
          envFrom:
            - secretRef:
                name: backstage-secret

            - secretRef:
                name: backstage-github-auth    
